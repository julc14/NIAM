<OverlayTabTemplate>
    <MudStack>
        <MudText>"@State.PersonsName @State.Scheme"</MudText>
    </MudStack>
    <MudButton Color="Color.Primary"
               Variant="Variant.Filled" 
               ButtonType="ButtonType.Submit" 
               OnClick="NameExoplanet"
               Style ="top:90%;left:78%"
               Size="Size.Large">
            Submit
    </MudButton>
</OverlayTabTemplate>

@code {
    [Inject]
    public HttpClient HttpClient { get; set; } = null!;

    [Parameter, EditorRequired]
    public NameExoplanetOperationState State { get; set; } = null!;

    private async Task NameExoplanet()
    {
        //await HttpClient.PostAsJsonAsync(NameExoplanetRequest);
    }
}
